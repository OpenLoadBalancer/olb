apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "olb.fullname" . }}-config
  labels:
    {{- include "olb.labels" . | nindent 4 }}
data:
  olb.yaml: |
{{- toYaml .Values.config | nindent 4 }}
